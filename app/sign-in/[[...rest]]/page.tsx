// Assuming the file is now named `page.tsx`
import Login from "@/components/login";
import { NextPage } from "next"; // Import NextPage for type definition

const Component: NextPage = () => {
  return <Login />;
};

export default Component;
